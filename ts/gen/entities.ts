// Code generated by protoc-gen-pd. DO NOT EDIT.

/**
 * What the local store is built from: one declaration per entity.
 *
 * Nothing here is behaviour. The store reads these together with the
 * protobuf-es descriptors -- which carry every field's name, number and type at
 * run time -- and implements reading, writing, normalizing and reconciling
 * once, for every entity there is.
 *
 * @module
 */

import type { EntityDesc } from '@lesomnus/payday/store'

import { AssetSchema } from './app/asset_pb.js'
import { AuditSchema } from './payday/audit_pb.js'
import { HolderSchema } from './payday/holder_pb.js'
import { OutboxSchema } from './payday/outbox_pb.js'
import { TenantSchema } from './payday/tenant_pb.js'

/** app.Asset, as the store holds it. */
export const Asset = {
	typeName: "app.Asset",
	schema: AssetSchema,
	domain: 7,
	version: "dateUpdated",
	refs: [{ field: "tenant", to: "payday.Tenant" }, { field: "keeper", to: "payday.Holder" }],
} as const satisfies EntityDesc

/** payday.Audit, as the store holds it. */
export const Audit = {
	typeName: "payday.Audit",
	schema: AuditSchema,
	domain: 3,
} as const satisfies EntityDesc

/** payday.Holder, as the store holds it. */
export const Holder = {
	typeName: "payday.Holder",
	schema: HolderSchema,
	domain: 2,
	version: "dateUpdated",
	refs: [{ field: "tenant", to: "payday.Tenant" }],
} as const satisfies EntityDesc

/** payday.Outbox, as the store holds it. */
export const Outbox = {
	typeName: "payday.Outbox",
	schema: OutboxSchema,
	domain: 4,
} as const satisfies EntityDesc

/** payday.Tenant, as the store holds it. */
export const Tenant = {
	typeName: "payday.Tenant",
	schema: TenantSchema,
	domain: 1,
	version: "dateUpdated",
} as const satisfies EntityDesc

/** Every entity of this app, which is what a store is opened over. */
export const entities = [Asset, Audit, Holder, Outbox, Tenant] as const

